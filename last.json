---
---

{
    "url": "{{ site.url }}{{ site.posts.first.url }}",
    "title": "{{ site.posts.first.title }}"{% if site.posts.first.images %},
    "image_url": "{{ site.url }}{{ site.posts.first.images.first.url }}"{% endif %}
}